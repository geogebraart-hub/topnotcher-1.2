# LET Genius Hub V22

Changes:
- True full-viewport Study Now overlay with no dashboard content visible underneath.
- Study Now uses the same floating, rounded sidebar treatment as the main app.
- Sidebar inset from viewport edges, rounded corners, depth/shadow, aligned bold labels and active state.
- Study Now question card sits immediately below the progress bar with compact breathing room.
- Rationale sits directly below answer choices with compact breathing room.
- Added browser-side PDF text extraction using pdfjs-dist in the AI material uploader.
- PDF, TXT, MD, and CSV accepted by AI material upload.

Deploy by replacing the repository contents and allowing Vercel to install the added pdfjs-dist dependency.
